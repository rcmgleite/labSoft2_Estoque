To run the tests, do:
  1) delete estoque_test.db on /bin folder if exists
  2) execute ./install.sh -test on labSoft2_Estoque
  3) enter test/ folder and execute "go test"

When running it multiple times, you will need to delete the old database every time (FIXME)
